import { ConditionformaterPipe } from './conditionformater.pipe';

describe('ConditionformaterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionformaterPipe();
    expect(pipe).toBeTruthy();
  });
});
